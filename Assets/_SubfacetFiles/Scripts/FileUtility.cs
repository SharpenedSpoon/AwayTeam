using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.IO;

public static class FileUtility {

	//public Dictionary<string, string> LoadFromFile(string fileName) {


		// From http://answers.unity3d.com/questions/279750/loading-data-from-a-txt-file-c.html


		//try {
		/*string line;
		var output = new Dictionary<string, string>;
		StreamReader theReader = new StreamReader(fileName, Encoding.Default);
		using (theReader) {
			do {
				LineManager = theReader.ReadLine();
				if (line != null) {
					//do stuff with the string, line.
					string[] entries = line.Split('=');
					if (entries.Length > 0) {
						output.Add(entries[0], entries[1]);
					}
				}
			}
			while (line != null);

			theReader.Close();
			return output;
		}*/
		//}

		/*catch(Exception e) {
			Debug.LogError("{0}\n", e.Message);
			return null;
		}*/
	//}

}